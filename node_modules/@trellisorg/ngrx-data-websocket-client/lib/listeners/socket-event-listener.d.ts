import type { Store } from '@ngrx/store';
import { SocketOp } from '@trellisorg/ngrx-data-websocket-core';
import { Observable } from 'rxjs';
import type { SocketActionFactory } from '../actions/socket-action-factory';
import type { NgrxDataWebsocketConfig } from '../utils/tokens';
export declare class SocketEventListener<T> {
    private socketActionFactory;
    private store;
    private _socket;
    private readonly _entityName;
    constructor(entityName: string, socketActionFactory: SocketActionFactory, store: Store);
    setupReservedEvents(): void;
    emit(event: SocketOp, crid: string, data: any): void;
    disconnect(): void;
    connect(config: NgrxDataWebsocketConfig, params: Record<string, string>): Observable<boolean>;
}
