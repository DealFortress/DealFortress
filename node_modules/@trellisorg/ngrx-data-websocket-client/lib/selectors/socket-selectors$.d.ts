import { Actions } from '@ngrx/effects';
import { Store } from '@ngrx/store';
import { SocketAction } from '@trellisorg/ngrx-data-websocket-core';
import { Observable } from 'rxjs';
import * as i0 from "@angular/core";
export interface SocketSelectors$<T> {
    readonly connected$: Observable<boolean>;
    readonly connecting$: Observable<boolean>;
}
export declare class SocketSelectors$Factory<T> {
    private store;
    private actions;
    constructor(store: Store, actions: Actions<SocketAction>);
    create<T>(entityName: string): SocketSelectors$<T>;
    static ɵfac: i0.ɵɵFactoryDeclaration<SocketSelectors$Factory<any>, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<SocketSelectors$Factory<any>>;
}
